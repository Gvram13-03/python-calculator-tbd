FROM jenkins/inbound-agent:latest

     USER root
     # Install Python 3 and pip
     RUN apt-get update && apt-get install -y python3 python3-pip
     # (Removed pip upgrade step due to Debian externally-managed environment restriction)
     # Switch back to non-root user for Jenkins
     USER jenkins